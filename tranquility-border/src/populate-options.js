export default function populateOptions(table) {
	return table.set({
		lightColor: "#ffffff",
		lightShortcut: ",",
		darkColor: "#1d1f21",
		darkShortcut: ".",
		boxShadow: "-2px -2px 6px 2px rgba(0, 0, 0, 0.3), 2px 2px 6px 2px rgba(0, 0, 0, 0.3)",
	});
}
