export default function populateOptions(table) {
	return table.set({
		shortcut: "S",
		shortcutHighlights: "H",
		color: "#f03b11",
		backgroundColor: "",
		underline: false,
		activated: true,
	});
}
